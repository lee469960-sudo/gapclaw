- [x] 1.1 扩展执行策略接口，支持绑定 MCP 的名称、标签、描述能力提示及通用资源名称识别
  - [x] 增加纯能力匹配与操作意图组合规则，避免概念问答误入任务路径
  - [x] 增加路由单元测试：隐含持仓查询、纯概念解释、显式资源名称
- [x] 1.2 在 Agent Runtime 入口读取已绑定 MCP 元数据，不连接 MCP、不加载工具目录
  - [x] 增加元数据读取失败时的安全降级
  - [x] 增加入口路由回归测试并确认现有 MCP 惰性加载不变
- [x] 2.1 实现未绑定显式 MCP/工具的任务路径停止
  - [x] 返回明确未绑定提示
  - [x] 写入 `mcp_not_bound` 终态指标，不调用未授权资源
- [x] 2.2 将能力匹配结果与现有 MCP LLM 语义路由衔接
  - [x] 验证已绑定 `okx-trader` 等资源继续经过服务端授权和后续工具选择
- [x] 3.1 运行聚焦路由、Agent Runtime、MCP 惰性加载回归测试
- [x] 3.2 运行完整 API 回归、编译和前端构建验证
- [x] 3.3 记录普通对话、隐含查询、显式绑定和未绑定资源的代表性指标与回滚方式
