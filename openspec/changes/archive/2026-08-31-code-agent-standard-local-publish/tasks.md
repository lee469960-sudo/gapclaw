## 1. Manifest Local 发布配置回退

- [x] 1.1 移除 Manifest 表单中的 Local 发布配置区域、默认值、JSON 校验与 payload 字段，并通过前端组件测试验证页面不再显示或提交这些字段。
- [x] 1.2 让 Manifest API、控制面校验与冻结 Run 契约忽略历史 local 发布字段；保留数据库列兼容但不将其用作新发布权限，并通过控制面回归测试验证。

## 2. 标准 local 发布档案与准备

- [x] 2.1 实现平台管理的项目标准 local 发布档案解析与 readiness 原因，固定发布凭据、入口策略、镜像、网络和验证计划，并通过拒绝/成功单元测试验证 Manifest 不能覆盖。
- [x] 2.2 实现仓库 CI/脚本/锁定文件扫描和受控应用依赖准备，仅支持实际存在的受支持锁定文件并写入 run-local 目录；通过 Git 状态与依赖准备测试验证不污染业务 Workspace。
- [x] 2.3 更新标准发布 runner 镜像以预装固定系统工具（dbt、jq、yq、ClickHouse client 等）并增加多架构构建/探测验证；缺失时给出稳定 preflight 原因。

## 3. 对话确认与单次发布

- [x] 3.1 将 local 发布任务改为读取标准档案，展示入口发现与依赖准备结果；唯一安全入口进入确认，多候选或未知入口不执行，并通过 Runtime 测试验证。
- [x] 3.2 在确认后执行一次冻结入口、保存脱敏证据并运行验证与清理；验证失败/状态未知不得自动重试或显示成功。

## 4. 回归验证与文档

- [x] 4.1 更新 `.env.example`、发布镜像文档与 UI 提示，明确平台档案、固定系统工具和锁定文件依赖准备的边界。
- [x] 4.2 运行后端控制面/发布/runner/Runtime 回归、前端构建、`git diff --check` 与 OpenSpec 严格校验，将实际结果记录到 `progress.md`。
