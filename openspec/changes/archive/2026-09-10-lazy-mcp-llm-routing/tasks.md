## 1. 路由候选与结构化决策

- [x] 1.1 实现从 Agent 绑定中筛选已授权且具备 `description` 或 `tags` 的 MCP 候选元数据，并验证空元数据 MCP 不进入候选集
- [x] 1.2 实现使用 Agent 配置 LLM 的受限 JSON 路由请求、响应解析、候选集交集校验和零/一/多 MCP 选择，并验证非法、超时或低置信度响应产生空选择而非全量回退
- [x] 1.3 将用户显式点名 MCP 作为路由输入强信号而不设名称硬编码或绕过授权校验，并验证未绑定/无权限 ID 被拒绝

## 2. 惰性目录发现与受限补选

- [x] 2.1 将初始系统工具目录构建改为只接收已验证选择集，并验证单域路由仅对被选 MCP 调用 `tools/list`，未选 MCP 不连接、不发现、不写缓存
- [x] 2.2 保持已选 MCP 内按工具名正确分派及单次运行会话复用，并验证跨域多选只发现所选多个 MCP、工具调用到实际声明工具的 MCP
- [x] 2.3 增加主 Agent 的结构化额外能力请求与已选 MCP 不可达/缺工具触发的补选流程，并验证每次仅加载新增选择、最多补选两次且达到上限后不再尝试
- [x] 2.4 将目录缓存限制为实际选中过的 MCP，并按 MCP 变更版本或 TTL 失效，验证缓存命中不执行 `tools/list` 且未选 MCP 不会因缓存被访问

## 3. 配置反馈与可观测性

- [x] 3.1 在 MCP/Agent 配置 API 与 UI 中显示缺少能力描述或标签的已绑定 MCP 不参与自动路由的状态，并验证补齐元数据后其可成为候选
- [x] 3.2 为初始路由和补选记录脱敏结构化事件（请求摘要、候选/选择 ID、理由、触发、补选序号、加载结果），并验证事件不含完整提示词、凭据或认证头

## 4. 回归验证

- [x] 4.1 为候选筛选、单域惰性选择、跨域多选、用户点名、非法路由响应和无全量回退添加单元/运行时回归测试并验证通过
- [x] 4.2 为补选触发与上限、工具分派、选中目录缓存、会话复用和路由审计添加回归测试并验证通过
- [x] 4.3 运行受影响 API/前端检查、Agent Runtime 测试、`compileall`、`openspec validate lazy-mcp-llm-routing --strict` 与 `git diff --check`，并验证全部通过
