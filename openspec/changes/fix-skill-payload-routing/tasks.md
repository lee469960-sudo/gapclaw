## 1. 完整请求复现

- [x] 1.1 添加用户完整 Skill 生成请求与真实风险操作的回归测试，证明分类和运行入口中的误判。

## 2. 修复

- [x] 2.1 统一风险判断与正文边界，恢复显式人工确认与真实操作保护，修正协议/Skill 资源识别。

## 3. 验证

- [x] 3.1 验证 Agent 入口进入 modular runner，Skill 读取和文件写入可执行，运行受影响回归、编译与 OpenSpec 验证并记录结果。
- [x] 3.2 验证隐含选股/筛选请求根据绑定能力元数据进入 React Engine，并保持概念问答走 chat。
- [x] 3.3 用绑定能力元数据语义匹配替代 MCP 专属操作词依赖，验证未知市场数据表达和知识问答边界。
- [x] 3.4 验证绑定 MCP 任务首轮 tool-free FINAL 会被纠偏并继续调用 MCP，且仅允许一次有界重试。
- [x] 3.5 用本地真实 tushareMcp 元数据复现“选出标的 + 条件列表”请求，修正稀疏能力描述导致的 chat 快速通道误判。
