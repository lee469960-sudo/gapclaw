---
name: openspec-archive-change
description: Archive an OpenSpec change. Do not use during the coding session.
compatibility: Requires openspec CLI.
---

Do **not** archive during this CodeAgent coding session. The platform runs `openspec archive` only after a successful seal.
